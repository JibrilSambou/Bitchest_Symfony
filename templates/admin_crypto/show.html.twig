{% extends 'base.html.twig' %}

{% block title %}Crypto{% endblock %}

{% block body %}
    <h1>Crypto</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ crypto.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ crypto.name }}</td>
            </tr>
            <tr>
                <th>Quantity</th>
                <td>{{ crypto.quantity }}</td>
            </tr>
            <tr>
                <th>Symbole</th>
                <td>{{ crypto.symbole }}</td>
            </tr>
            <tr>
                <th>Price</th>
                <td>{{ crypto.price }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_admin_crypto_index') }}">back to list</a>

    <a href="{{ path('app_admin_crypto_edit', {'id': crypto.id}) }}">edit</a>

    {{ include('admin_crypto/_delete_form.html.twig') }}
{% endblock %}
