{% extends 'base.html.twig' %}

{% block title %}Wallet{% endblock %}

{% block body %}
    <h1>Wallet</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ wallet.id }}</td>
            </tr>
            <tr>
                <th>Balance</th>
                <td>{{ wallet.balance }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_admin_wallet_index') }}">back to list</a>

    <a href="{{ path('app_admin_wallet_edit', {'id': wallet.id}) }}">edit</a>

    {{ include('admin_wallet/_delete_form.html.twig') }}
{% endblock %}
